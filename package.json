{
  "name": "street-world",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run --workspace @street-world/web dev & npm run --workspace @street-world/api start:dev",
    "dev:web": "npm run --workspace @street-world/web dev",
    "dev:api": "npm run --workspace @street-world/api start:dev",
    "build": "npm run --workspace @street-world/web build && npm run --workspace @street-world/api build",
    "lint": "npm run --workspace @street-world/web lint && npm run --workspace @street-world/api lint"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/express": "^5.0.6"
  }
}
